CREATE TABLE employeesqlc (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    age INT NOT NULL,
    department VARCHAR(50) NOT NULL,
    salary DOUBLE NOT NULL,
    city VARCHAR(50) NOT NULL
);